"""Comprehensive unit tests for CLI functionality."""

import os\nimport json\nimport tempfile\nimport subprocess\nfrom pathlib import Path\nfrom unittest.mock import Mock, patch, MagicMock, call\nfrom io import StringIO\n\nimport pytest\nfrom click.testing import CliRunner\n\nfrom daglab.cli import (\n    main, init, run, discover, doctor, clean, stats, \n    scaffold, export, migrate, dev\n)\nfrom daglab.helpers import Config\nfrom daglab.core import DAG, Node, Edge\n\n\nclass TestCLICore:\n    \"\"\"Test core CLI functionality.\"\"\"\n    \n    def test_main_help(self):\n        \"\"\"Test main CLI help command.\"\"\"\n        runner = CliRunner()\n        result = runner.invoke(main, ['--help'])\n        \n        assert result.exit_code == 0\n        assert \"DagLab\" in result.output\n        assert \"Commands:\" in result.output\n        \n        # Check that all main commands are listed\n        expected_commands = ['init', 'run', 'discover', 'doctor', 'clean', 'stats']\n        for cmd in expected_commands:\n            assert cmd in result.output\n    \n    def test_main_version(self):\n        \"\"\"Test version display.\"\"\"\n        runner = CliRunner()\n        result = runner.invoke(main, ['--version'])\n        \n        assert result.exit_code == 0\n        assert \"version\" in result.output.lower() or \"0.1.0\" in result.output\n    \n    def test_main_verbose_flag(self):\n        \"\"\"Test verbose flag functionality.\"\"\"\n        runner = CliRunner()\n        \n        # Test with valid subcommand and verbose flag\n        with patch('daglab.cli.doctor') as mock_doctor:\n            mock_doctor.return_value = None\n            result = runner.invoke(main, ['--verbose', 'doctor'])\n            \n            # Should not fail due to verbose flag\n            assert result.exit_code == 0 or \"verbose\" in result.output.lower()\n    \n    def test_invalid_command(self):\n        \"\"\"Test invalid command handling.\"\"\"\n        runner = CliRunner()\n        result = runner.invoke(main, ['invalid_command'])\n        \n        assert result.exit_code != 0\n        assert \"No such command\" in result.output or \"invalid\" in result.output.lower()\n\n\nclass TestInitCommand:\n    \"\"\"Test init command functionality.\"\"\"\n    \n    def test_init_basic(self, temp_dir):\n        \"\"\"Test basic project initialization.\"\"\"\n        runner = CliRunner()\n        \n        with runner.isolated_filesystem():\n            result = runner.invoke(init, ['test_project'])\n            \n            # Should succeed\n            assert result.exit_code == 0\n            \n            # Check that project directory was created\n            project_dir = Path('test_project')\n            assert project_dir.exists()\n            assert project_dir.is_dir()\n    \n    def test_init_with_template(self, temp_dir):\n        \"\"\"Test initialization with template.\"\"\"\n        runner = CliRunner()\n        \n        with runner.isolated_filesystem():\n            result = runner.invoke(init, [\n                'test_project',\n                '--template', 'minimal'\n            ])\n            \n            assert result.exit_code == 0\n            \n            project_dir = Path('test_project')\n            assert project_dir.exists()\n    \n    def test_init_existing_directory(self, temp_dir):\n        \"\"\"Test initialization in existing directory.\"\"\"\n        runner = CliRunner()\n        \n        with runner.isolated_filesystem():\n            # Create directory first\n            os.makedirs('existing_project')\n            \n            result = runner.invoke(init, ['existing_project'])\n            \n            # Should handle existing directory gracefully\n            # (behavior depends on implementation)\n            assert result.exit_code == 0 or \"exists\" in result.output.lower()\n    \n    def test_init_with_config(self, temp_dir):\n        \"\"\"Test initialization with configuration options.\"\"\"\n        runner = CliRunner()\n        \n        with runner.isolated_filesystem():\n            result = runner.invoke(init, [\n                'config_project',\n                '--template', 'default',\n                '--python-version', '3.9'\n            ])\n            \n            assert result.exit_code == 0\n    \n    @patch('daglab.cli.Path.mkdir')\n    def test_init_permission_error(self, mock_mkdir):\n        \"\"\"Test init with permission errors.\"\"\"\n        mock_mkdir.side_effect = PermissionError(\"Permission denied\")\n        \n        runner = CliRunner()\n        result = runner.invoke(init, ['restricted_project'])\n        \n        assert result.exit_code != 0\n        assert \"permission\" in result.output.lower() or \"error\" in result.output.lower()\n\n\nclass TestRunCommand:\n    \"\"\"Test run command functionality.\"\"\"\n    \n    def test_run_help(self):\n        \"\"\"Test run command help.\"\"\"\n        runner = CliRunner()\n        result = runner.invoke(run, ['--help'])\n        \n        assert result.exit_code == 0\n        assert \"run\" in result.output.lower()\n    \n    @patch('daglab.cli.subprocess.run')\n    def test_run_dag_file(self, mock_subprocess, temp_dir):\n        \"\"\"Test running a DAG file.\"\"\"\n        # Create mock DAG file\n        dag_file = temp_dir / \"test_dag.py\"\n        dag_content = \"\"\"\nfrom daglab.core import DAG, Node\n\ndag = DAG(name=\"test_dag\")\nnode = Node(name=\"test_node\", task_type=\"python\")\ndag.add_node(node)\n\"\"\"\n        dag_file.write_text(dag_content)\n        \n        mock_subprocess.return_value = Mock(returncode=0, stdout=\"Success\", stderr=\"\")\n        \n        runner = CliRunner()\n        result = runner.invoke(run, [str(dag_file)])\n        \n        # Should attempt to run the DAG\n        assert result.exit_code == 0 or mock_subprocess.called\n    \n    def test_run_missing_file(self):\n        \"\"\"Test run with missing DAG file.\"\"\"\n        runner = CliRunner()\n        result = runner.invoke(run, ['nonexistent_dag.py'])\n        \n        assert result.exit_code != 0\n        assert \"not found\" in result.output.lower() or \"error\" in result.output.lower()\n    \n    @patch('daglab.cli.subprocess.run')\n    def test_run_with_config(self, mock_subprocess, temp_dir):\n        \"\"\"Test run with configuration file.\"\"\"\n        # Create config file\n        config_file = temp_dir / \"config.yaml\"\n        config_content = \"\"\"\ndagster:\n  host: localhost\n  port: 3000\nenvironment: test\n\"\"\"\n        config_file.write_text(config_content)\n        \n        # Create DAG file\n        dag_file = temp_dir / \"test_dag.py\"\n        dag_file.write_text(\"# Test DAG\")\n        \n        mock_subprocess.return_value = Mock(returncode=0)\n        \n        runner = CliRunner()\n        result = runner.invoke(run, [\n            str(dag_file),\n            '--config', str(config_file)\n        ])\n        \n        assert result.exit_code == 0 or mock_subprocess.called\n    \n    @patch('daglab.cli.subprocess.run')\n    def test_run_dry_run(self, mock_subprocess, temp_dir):\n        \"\"\"Test dry run functionality.\"\"\"\n        dag_file = temp_dir / \"test_dag.py\"\n        dag_file.write_text(\"# Test DAG\")\n        \n        runner = CliRunner()\n        result = runner.invoke(run, [\n            str(dag_file),\n            '--dry-run'\n        ])\n        \n        # Dry run should not execute actual subprocess\n        assert not mock_subprocess.called or \"dry\" in result.output.lower()\n        assert result.exit_code == 0\n\n\nclass TestDiscoverCommand:\n    \"\"\"Test discover command functionality.\"\"\"\n    \n    def test_discover_help(self):\n        \"\"\"Test discover command help.\"\"\"\n        runner = CliRunner()\n        result = runner.invoke(discover, ['--help'])\n        \n        assert result.exit_code == 0\n        assert \"discover\" in result.output.lower()\n    \n    @patch('daglab.cli.DagsterClientSync')\n    def test_discover_basic(self, mock_client_class, temp_dir):\n        \"\"\"Test basic discovery functionality.\"\"\"\n        # Mock Dagster client\n        mock_client = Mock()\n        mock_client.health_check.return_value = True\n        mock_client.query.return_value = {\n            \"repositoriesOrError\": {\n                \"nodes\": [\n                    {\n                        \"name\": \"test_repo\",\n                        \"location\": {\"name\": \"test_location\"},\n                        \"jobs\": [\n                            {\"name\": \"test_job\", \"description\": \"Test job\"}\n                        ]\n                    }\n                ]\n            }\n        }\n        mock_client_class.return_value = mock_client\n        \n        runner = CliRunner()\n        \n        with runner.isolated_filesystem():\n            result = runner.invoke(discover, [\n                '--output-dir', 'notebooks',\n                '--dagster-url', 'http://localhost:3000'\n            ])\n            \n            # Should succeed and call Dagster client\n            assert result.exit_code == 0 or mock_client.health_check.called\n    \n    @patch('daglab.cli.DagsterClientSync')\n    def test_discover_connection_error(self, mock_client_class):\n        \"\"\"Test discover with connection error.\"\"\"\n        mock_client = Mock()\n        mock_client.health_check.return_value = False\n        mock_client_class.return_value = mock_client\n        \n        runner = CliRunner()\n        result = runner.invoke(discover, [\n            '--dagster-url', 'http://invalid:9999'\n        ])\n        \n        # Should handle connection error gracefully\n        assert \"connection\" in result.output.lower() or \"error\" in result.output.lower()\n    \n    @patch('daglab.cli.DagsterClientSync')\n    def test_discover_with_filters(self, mock_client_class, temp_dir):\n        \"\"\"Test discover with repository filters.\"\"\"\n        mock_client = Mock()\n        mock_client.health_check.return_value = True\n        mock_client.query.return_value = {\n            \"repositoriesOrError\": {\"nodes\": []}\n        }\n        mock_client_class.return_value = mock_client\n        \n        runner = CliRunner()\n        \n        with runner.isolated_filesystem():\n            result = runner.invoke(discover, [\n                '--repository-filter', 'test_*',\n                '--job-filter', '*_etl'\n            ])\n            \n            assert result.exit_code == 0\n    \n    @patch('daglab.cli._async_discover_jobs')\n    def test_discover_async_mode(self, mock_async_discover):\n        \"\"\"Test async discovery mode.\"\"\"\n        mock_async_discover.return_value = \"Discovery completed\"\n        \n        runner = CliRunner()\n        result = runner.invoke(discover, ['--async'])\n        \n        # Should handle async mode\n        assert result.exit_code == 0 or mock_async_discover.called\n\n\nclass TestDoctorCommand:\n    \"\"\"Test doctor command functionality.\"\"\"\n    \n    def test_doctor_basic(self):\n        \"\"\"Test basic doctor functionality.\"\"\"\n        runner = CliRunner()\n        result = runner.invoke(doctor)\n        \n        assert result.exit_code == 0\n        # Should include system checks\n        assert \"check\" in result.output.lower() or \"system\" in result.output.lower()\n    \n    @patch('daglab.cli.subprocess.run')\n    def test_doctor_python_check(self, mock_subprocess):\n        \"\"\"Test Python version check.\"\"\"\n        mock_subprocess.return_value = Mock(\n            returncode=0,\n            stdout=\"Python 3.9.0\",\n            stderr=\"\"\n        )\n        \n        runner = CliRunner()\n        result = runner.invoke(doctor)\n        \n        assert result.exit_code == 0\n        # Should check Python version\n        assert \"python\" in result.output.lower()\n    \n    @patch('daglab.cli.shutil.which')\n    def test_doctor_missing_dependencies(self, mock_which):\n        \"\"\"Test doctor with missing dependencies.\"\"\"\n        mock_which.return_value = None  # Simulate missing command\n        \n        runner = CliRunner()\n        result = runner.invoke(doctor)\n        \n        # Should report missing dependencies\n        assert \"missing\" in result.output.lower() or \"not found\" in result.output.lower()\n    \n    @patch('daglab.cli.requests.get')\n    def test_doctor_network_check(self, mock_requests):\n        \"\"\"Test network connectivity check.\"\"\"\n        mock_response = Mock()\n        mock_response.status_code = 200\n        mock_requests.return_value = mock_response\n        \n        runner = CliRunner()\n        result = runner.invoke(doctor, ['--check-network'])\n        \n        assert result.exit_code == 0\n    \n    def test_doctor_fix_permissions(self, temp_dir):\n        \"\"\"Test doctor permission fixing.\"\"\"\n        runner = CliRunner()\n        \n        with runner.isolated_filesystem():\n            result = runner.invoke(doctor, ['--fix-permissions'])\n            \n            # Should attempt to fix permissions\n            assert result.exit_code == 0\n\n\nclass TestCleanCommand:\n    \"\"\"Test clean command functionality.\"\"\"\n    \n    def test_clean_basic(self, temp_dir):\n        \"\"\"Test basic clean functionality.\"\"\"\n        # Create some temporary files\n        (temp_dir / \"temp_file.tmp\").write_text(\"temp content\")\n        (temp_dir / \".cache\").mkdir()\n        (temp_dir / \"__pycache__\").mkdir()\n        \n        runner = CliRunner()\n        \n        with patch('daglab.cli.Path.cwd', return_value=temp_dir):\n            result = runner.invoke(clean)\n            \n            assert result.exit_code == 0\n            assert \"clean\" in result.output.lower()\n    \n    def test_clean_dry_run(self, temp_dir):\n        \"\"\"Test clean dry run.\"\"\"\n        # Create temporary files\n        temp_file = temp_dir / \"temp_file.tmp\"\n        temp_file.write_text(\"temp content\")\n        \n        runner = CliRunner()\n        \n        with patch('daglab.cli.Path.cwd', return_value=temp_dir):\n            result = runner.invoke(clean, ['--dry-run'])\n            \n            assert result.exit_code == 0\n            # File should still exist in dry run\n            assert temp_file.exists()\n    \n    def test_clean_specific_patterns(self, temp_dir):\n        \"\"\"Test cleaning specific patterns.\"\"\"\n        # Create files with different patterns\n        (temp_dir / \"test.log\").write_text(\"log content\")\n        (temp_dir / \"data.tmp\").write_text(\"temp data\")\n        (temp_dir / \"important.txt\").write_text(\"important\")\n        \n        runner = CliRunner()\n        \n        with patch('daglab.cli.Path.cwd', return_value=temp_dir):\n            result = runner.invoke(clean, ['--pattern', '*.tmp'])\n            \n            assert result.exit_code == 0\n    \n    def test_clean_force(self, temp_dir):\n        \"\"\"Test clean with force flag.\"\"\"\n        # Create read-only file\n        readonly_file = temp_dir / \"readonly.tmp\"\n        readonly_file.write_text(\"readonly content\")\n        \n        runner = CliRunner()\n        \n        with patch('daglab.cli.Path.cwd', return_value=temp_dir):\n            result = runner.invoke(clean, ['--force'])\n            \n            assert result.exit_code == 0\n\n\nclass TestStatsCommand:\n    \"\"\"Test stats command functionality.\"\"\"\n    \n    def test_stats_basic(self, temp_dir):\n        \"\"\"Test basic stats functionality.\"\"\"\n        # Create project structure\n        project_dir = temp_dir / \"project\"\n        project_dir.mkdir()\n        (project_dir / \"dag1.py\").write_text(\"# DAG 1\")\n        (project_dir / \"dag2.py\").write_text(\"# DAG 2\")\n        \n        runner = CliRunner()\n        \n        with patch('daglab.cli.Path.cwd', return_value=project_dir):\n            result = runner.invoke(stats)\n            \n            assert result.exit_code == 0\n            assert \"stats\" in result.output.lower() or \"statistics\" in result.output.lower()\n    \n    def test_stats_detailed(self, temp_dir):\n        \"\"\"Test detailed stats.\"\"\"\n        runner = CliRunner()\n        result = runner.invoke(stats, ['--detailed'])\n        \n        assert result.exit_code == 0\n    \n    def test_stats_json_output(self, temp_dir):\n        \"\"\"Test JSON output format.\"\"\"\n        runner = CliRunner()\n        result = runner.invoke(stats, ['--format', 'json'])\n        \n        assert result.exit_code == 0\n        \n        # Try to parse output as JSON\n        try:\n            if result.output.strip():\n                json.loads(result.output)\n        except json.JSONDecodeError:\n            # Output might not be pure JSON if there are other messages\n            pass\n    \n    def test_stats_specific_directory(self, temp_dir):\n        \"\"\"Test stats for specific directory.\"\"\"\n        target_dir = temp_dir / \"target\"\n        target_dir.mkdir()\n        \n        runner = CliRunner()\n        result = runner.invoke(stats, ['--directory', str(target_dir)])\n        \n        assert result.exit_code == 0\n\n\nclass TestScaffoldCommand:\n    \"\"\"Test scaffold command functionality.\"\"\"\n    \n    def test_scaffold_help(self):\n        \"\"\"Test scaffold command help.\"\"\"\n        runner = CliRunner()\n        result = runner.invoke(scaffold, ['--help'])\n        \n        assert result.exit_code == 0\n        assert \"scaffold\" in result.output.lower()\n    \n    def test_scaffold_dag(self, temp_dir):\n        \"\"\"Test scaffolding a new DAG.\"\"\"\n        runner = CliRunner()\n        \n        with runner.isolated_filesystem():\n            result = runner.invoke(scaffold, [\n                'dag',\n                '--name', 'test_dag',\n                '--output', 'test_dag.py'\n            ])\n            \n            assert result.exit_code == 0\n            \n            # Check if DAG file was created\n            dag_file = Path('test_dag.py')\n            if dag_file.exists():\n                content = dag_file.read_text()\n                assert \"test_dag\" in content\n    \n    def test_scaffold_pipeline(self, temp_dir):\n        \"\"\"Test scaffolding a pipeline.\"\"\"\n        runner = CliRunner()\n        \n        with runner.isolated_filesystem():\n            result = runner.invoke(scaffold, [\n                'pipeline',\n                '--name', 'etl_pipeline',\n                '--template', 'etl'\n            ])\n            \n            assert result.exit_code == 0\n    \n    def test_scaffold_existing_file(self, temp_dir):\n        \"\"\"Test scaffold with existing file.\"\"\"\n        runner = CliRunner()\n        \n        with runner.isolated_filesystem():\n            # Create existing file\n            Path('existing.py').write_text(\"# Existing file\")\n            \n            result = runner.invoke(scaffold, [\n                'dag',\n                '--name', 'test_dag',\n                '--output', 'existing.py'\n            ])\n            \n            # Should handle existing file\n            assert \"exists\" in result.output.lower() or result.exit_code != 0\n    \n    def test_scaffold_force_overwrite(self, temp_dir):\n        \"\"\"Test scaffold with force overwrite.\"\"\"\n        runner = CliRunner()\n        \n        with runner.isolated_filesystem():\n            # Create existing file\n            existing_file = Path('existing.py')\n            existing_file.write_text(\"# Existing file\")\n            \n            result = runner.invoke(scaffold, [\n                'dag',\n                '--name', 'test_dag',\n                '--output', 'existing.py',\n                '--force'\n            ])\n            \n            assert result.exit_code == 0\n\n\nclass TestExportCommand:\n    \"\"\"Test export command functionality.\"\"\"\n    \n    def test_export_help(self):\n        \"\"\"Test export command help.\"\"\"\n        runner = CliRunner()\n        result = runner.invoke(export, ['--help'])\n        \n        assert result.exit_code == 0\n        assert \"export\" in result.output.lower()\n    \n    @patch('daglab.cli.DagsterClientSync')\n    def test_export_dagster(self, mock_client_class, temp_dir):\n        \"\"\"Test exporting from Dagster.\"\"\"\n        mock_client = Mock()\n        mock_client.health_check.return_value = True\n        mock_client.query.return_value = {\n            \"repositoriesOrError\": {\"nodes\": []}\n        }\n        mock_client_class.return_value = mock_client\n        \n        runner = CliRunner()\n        \n        with runner.isolated_filesystem():\n            result = runner.invoke(export, [\n                '--source', 'dagster',\n                '--output', 'exported_dags',\n                '--format', 'python'\n            ])\n            \n            assert result.exit_code == 0\n    \n    def test_export_notebooks(self, temp_dir):\n        \"\"\"Test exporting notebooks.\"\"\"\n        # Create sample notebook\n        notebook_dir = temp_dir / \"notebooks\"\n        notebook_dir.mkdir()\n        \n        notebook_content = {\n            \"cells\": [\n                {\n                    \"cell_type\": \"code\",\n                    \"source\": [\"print('Hello, World!')\"]\n                }\n            ],\n            \"metadata\": {},\n            \"nbformat\": 4,\n            \"nbformat_minor\": 4\n        }\n        \n        notebook_file = notebook_dir / \"test.ipynb\"\n        notebook_file.write_text(json.dumps(notebook_content))\n        \n        runner = CliRunner()\n        \n        with patch('daglab.cli.Path.cwd', return_value=temp_dir):\n            result = runner.invoke(export, [\n                '--source', 'notebooks',\n                '--input-dir', str(notebook_dir),\n                '--format', 'python'\n            ])\n            \n            assert result.exit_code == 0\n    \n    def test_export_multiple_formats(self, temp_dir):\n        \"\"\"Test exporting to multiple formats.\"\"\"\n        runner = CliRunner()\n        \n        with runner.isolated_filesystem():\n            result = runner.invoke(export, [\n                '--source', 'local',\n                '--format', 'yaml',\n                '--format', 'json'\n            ])\n            \n            assert result.exit_code == 0\n\n\nclass TestMigrateCommand:\n    \"\"\"Test migrate command functionality.\"\"\"\n    \n    def test_migrate_help(self):\n        \"\"\"Test migrate command help.\"\"\"\n        runner = CliRunner()\n        result = runner.invoke(migrate, ['--help'])\n        \n        assert result.exit_code == 0\n        assert \"migrate\" in result.output.lower()\n    \n    def test_migrate_version_check(self):\n        \"\"\"Test migration version check.\"\"\"\n        runner = CliRunner()\n        result = runner.invoke(migrate, ['--check-version'])\n        \n        assert result.exit_code == 0\n        assert \"version\" in result.output.lower()\n    \n    def test_migrate_dry_run(self, temp_dir):\n        \"\"\"Test migration dry run.\"\"\"\n        runner = CliRunner()\n        \n        with runner.isolated_filesystem():\n            result = runner.invoke(migrate, [\n                '--from-version', '0.1.0',\n                '--to-version', '0.2.0',\n                '--dry-run'\n            ])\n            \n            # Should not modify files in dry run\n            assert result.exit_code == 0\n            assert \"dry\" in result.output.lower() or \"would\" in result.output.lower()\n    \n    def test_migrate_backup(self, temp_dir):\n        \"\"\"Test migration with backup.\"\"\"\n        runner = CliRunner()\n        \n        with runner.isolated_filesystem():\n            # Create some files to migrate\n            Path('old_config.yaml').write_text(\"old: config\")\n            \n            result = runner.invoke(migrate, [\n                '--backup',\n                '--backup-dir', 'backups'\n            ])\n            \n            assert result.exit_code == 0\n\n\nclass TestDevCommand:\n    \"\"\"Test dev command functionality.\"\"\"\n    \n    def test_dev_help(self):\n        \"\"\"Test dev command help.\"\"\"\n        runner = CliRunner()\n        result = runner.invoke(dev, ['--help'])\n        \n        assert result.exit_code == 0\n        assert \"dev\" in result.output.lower() or \"development\" in result.output.lower()\n    \n    @patch('daglab.cli.subprocess.Popen')\n    def test_dev_server_start(self, mock_popen):\n        \"\"\"Test starting development server.\"\"\"\n        mock_process = Mock()\n        mock_process.poll.return_value = None  # Process running\n        mock_popen.return_value = mock_process\n        \n        runner = CliRunner()\n        result = runner.invoke(dev, ['start'])\n        \n        # Should attempt to start server\n        assert result.exit_code == 0 or mock_popen.called\n    \n    def test_dev_validate(self, temp_dir):\n        \"\"\"Test development validation.\"\"\"\n        # Create sample DAG file\n        dag_file = temp_dir / \"test_dag.py\"\n        dag_content = \"\"\"\nfrom daglab.core import DAG, Node\n\ndag = DAG(name=\"test_dag\")\nnode = Node(name=\"test_node\", task_type=\"python\")\ndag.add_node(node)\n\"\"\"\n        dag_file.write_text(dag_content)\n        \n        runner = CliRunner()\n        \n        with patch('daglab.cli.Path.cwd', return_value=temp_dir):\n            result = runner.invoke(dev, ['validate'])\n            \n            assert result.exit_code == 0\n    \n    def test_dev_watch_mode(self, temp_dir):\n        \"\"\"Test development watch mode.\"\"\"\n        runner = CliRunner()\n        \n        # Use timeout to prevent infinite watching\n        with patch('daglab.cli.time.sleep', side_effect=KeyboardInterrupt):\n            result = runner.invoke(dev, ['watch', '--timeout', '1'])\n            \n            # Should handle watch mode\n            assert \"watch\" in result.output.lower() or \"interrupted\" in result.output.lower()\n\n\nclass TestCLIUtilities:\n    \"\"\"Test CLI utility functions and error handling.\"\"\"\n    \n    def test_error_handling(self):\n        \"\"\"Test general error handling.\"\"\"\n        runner = CliRunner()\n        \n        # Test with invalid arguments\n        result = runner.invoke(run, ['--invalid-flag'])\n        assert result.exit_code != 0\n    \n    def test_config_loading(self, temp_dir):\n        \"\"\"Test configuration file loading.\"\"\"\n        config_file = temp_dir / \"daglab.yaml\"\n        config_content = \"\"\"\ndagster:\n  host: localhost\n  port: 3000\nlogging:\n  level: INFO\n\"\"\"\n        config_file.write_text(config_content)\n        \n        runner = CliRunner()\n        \n        # Test commands that should load config\n        with patch('daglab.cli.Path.cwd', return_value=temp_dir):\n            result = runner.invoke(doctor)\n            assert result.exit_code == 0\n    \n    def test_environment_variables(self):\n        \"\"\"Test environment variable handling.\"\"\"\n        runner = CliRunner()\n        \n        # Test with environment variables\n        env_vars = {\n            'DAGLAB_DEBUG': 'true',\n            'DAGLAB_LOG_LEVEL': 'DEBUG'\n        }\n        \n        with patch.dict(os.environ, env_vars):\n            result = runner.invoke(doctor)\n            assert result.exit_code == 0\n    \n    def test_signal_handling(self):\n        \"\"\"Test signal handling (Ctrl+C).\"\"\"\n        runner = CliRunner()\n        \n        # Simulate KeyboardInterrupt\n        with patch('daglab.cli.time.sleep', side_effect=KeyboardInterrupt):\n            result = runner.invoke(dev, ['watch'])\n            \n            # Should handle interruption gracefully\n            assert \"interrupted\" in result.output.lower() or result.exit_code != 0\n    \n    def test_logging_configuration(self, temp_dir):\n        \"\"\"Test logging configuration.\"\"\"\n        runner = CliRunner()\n        \n        with patch('daglab.cli.Path.cwd', return_value=temp_dir):\n            result = runner.invoke(main, ['--verbose', 'doctor'])\n            \n            # Verbose mode should enable detailed logging\n            assert result.exit_code == 0\n    \n    @patch('daglab.cli.sys.exit')\n    def test_graceful_shutdown(self, mock_exit):\n        \"\"\"Test graceful shutdown on errors.\"\"\"\n        runner = CliRunner()\n        \n        # Simulate fatal error\n        with patch('daglab.cli.main', side_effect=Exception(\"Fatal error\")):\n            result = runner.invoke(main, ['doctor'])\n            \n            # Should handle fatal errors\n            assert \"error\" in result.output.lower() or mock_exit.called